import 'package:get_it/get_it.dart';

final GetIt serviceLocator = GetIt.instance;

void registerDependencies(GetIt locator) {
  // Registrations are added here as concrete application dependencies appear.
}
